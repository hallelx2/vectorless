{
  "installCommand": "cd ../.. && pnpm install && pnpm --filter=@vectorless/shared build && pnpm --filter=vectorless build && pnpm --filter=@vectorless/mcp-tools build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 300
    }
  }
}
