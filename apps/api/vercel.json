{
  "installCommand": "cd ../.. && pnpm install && cd apps/api && pnpm --filter=@vectorless/shared build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 300
    }
  }
}
